import React from 'react';
import ProductCard from '../components/ProductCard';

const Cart = () => {
    return (
        <div>
            <ProductCard/>
        </div>
    );
};

export default Cart;